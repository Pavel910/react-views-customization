import React from 'react';
import ReactDOM from 'react-dom';
import { App } from './view_manager_v2';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
